#include <stdio.h>
#include <math.h>

// #define MAX(a, b) ({(typeof(_a)=a;typeof(_b)=b;  _a >  _b) ? a : b})

struct node
{
    int data;
    struct node *next;
} * p;

int main(int argc, char const *argv[])
{
    // int x = 'a'+1.6 ;

    // float *p ;
    // // p = (float *)3.14;

    // printf("%p\\",p);
    // char a[] = "";
    // int a[2][3], *p[3];
    // p[0] = a;
    // p[0] = a[0][0];
    

    // printf("%d\n", sizeof(struct node));
    printf("%d\n", 5/2);
    // int i=0;
    // do
    // {
    //     /* code */
    //     printf("%d\n",i);
    // } while (i++);
    //  printf("%d\n",i);
    //  double y;
    //  y=sqrt(x>=0?x:-x);
    //  printf("%lf \n",y);

    //  if (x>i)
    //     printf("%lf \n",y);
    //  else
    //     printf("%lf \n",y);

    return 0;
}
